direction = point_direction(x, y, destinationX, destinationY);
depth = -299;
initialX = x;
initialY = y;

rotateAngle = 0;

initPathDist = pathDistance;
imageSize = 0; // increment between 0 and .5 to shrink image